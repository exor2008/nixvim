## Personal nixvim assembly

### Installation

- Install NIX: https://nix.dev/install-nix.html or `curl -L https://nixos.org/nix/install | sh -s -- --daemon`
- Run `nix profile install --extra-experimental-features github:exor2008/nixvim#nvim`
